# evals/functional

Per-tool behavioral checks: does each WebMCP tool do what its contract says for well-formed and ill-formed input (schema-valid accept, descriptive error-return for invalid)? Written when tools arrive.

**Phase 0 status:** intentionally empty — no business tools exist yet. First
entries arrive with Phase 1 tool contracts.
